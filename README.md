# Merkle_Tree

A simple Merkle tree implementation. To be integrated with ﬁle version history tool which creates incremental backups of ﬁles that can be restored at a later date.

Current Status: Version1(Very Basic) functions implemented.